server:
  port: 80

mybatis:
  mapperLocations: classpath:mapper/*.xml
  # type-aliases-package: cn.ryanalexander.alibaba.domain    # 不是很靠谱。、。。


spring:
  application:
    name: nacos-order-consumer


  cloud:
    alibaba:
      seata:
        #自定义事务组名称需要与seata-server中的对应
        tx-service-group: fuck_tx_group
    nacos:
      discovery:
        server-addr: 1.15.235.161:8848
    sentinel:
      transport:
        #配置Sentinel dashboard地址
        dashboard: 1.15.235.161:8080
        #默认8719端口，假如被占用会自动从8719开始依次+1扫描,直至找到未被占用的端口
        port: 8719
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://1.15.235.161:3306/seata_order?characterEncoding=utf8&connectTimeout=1000&socketTimeout=3000&autoReconnect=true
    username: ryan
    password: Rzx8170069:)
#      datasource: #<---------------------------关注点，添加Nacos数据源配置
#        ds1:
#          nacos:
#            server-addr: 1.15.235.161:8848
#            dataId: sentinel-service
#            groupId: DEFAULT_GROUP
#            data-type: json
#            rule-type: flow
# 每次 任何规则我都需要手写成JSON来嘛？？？？ 如果需要修改一下就要在nocos里边找个几百年？
# 不同层次 不同接口的这些配置文件一大堆堆积到这里 怎么看？？
# 为什么不能够在sentinel终端操作 然后存到 nacos datasource里边呢？

management:
  endpoints:
    web:
      exposure:
        include: '*'

# 激活Sentinel对Feign的支持
feign:
  sentinel:
    enabled: true

