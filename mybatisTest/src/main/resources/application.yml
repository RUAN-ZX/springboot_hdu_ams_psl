spring:
  # 配置数据源信息
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 偶尔出现java.sql.SQLNonTransientConnectionException问题
    url: jdbc:mysql://1.15.235.161:3306/mybatis_plus?characterEncoding=utf8&connectTimeout=1000&socketTimeout=3000&autoReconnect=true&serverTimezone=GMT%2B8&useSSL=false
    username: ryan
    password: Rzx8170069:)

mybatis-plus:  n
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  # 设置MyBatis-Plus的全局配置
  global-config:
    db-config:
      # 设置实体类所对应的表的统一前缀
      table-prefix: t_
      # 设置统一的主键生成策略
      id-type: auto
  # 配置类型别名所对应的包
  type-aliases-package: cn.ryanalexander.alibaba.pojo
  # 扫描通用枚举的包
  type-enums-package: cn.ryanalexander.alibaba.enums
  mapper-locations: classpath:mapper/*.xml

# 网络地址 统一配置 nacos！
#logging:
#  config: https://stea.ryanalexander.cn/psl/log4j2.yml
#  config: classpath:log4j2.yml

